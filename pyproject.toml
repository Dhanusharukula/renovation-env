[project]
name = "renovation-env"
version = "0.1.0"
description = "AI Renovation Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0"
]

[tool.openenv]
entrypoint = "server.app:main"
